.app.profile
  .user-info
    .user-avatar

:javascript
  // Page-specific code
  $(document).ready(function (argument) {

    var user = new Profile({
    });

    var profileView = new ProfileView({
      model: user
    });

    console.log('User:', user);

    $('.user-info').append(profileView.el);

    profileView.render();

  });